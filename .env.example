# Server-side only. Never prefix these with NEXT_PUBLIC_ — they must not
# reach the browser bundle. /api/ask-adam is the only place that should
# read them.

# TODO(ask-adam-backend): AWS credentials/region for Bedrock, once wired up.
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
BEDROCK_MODEL_ID=

# TODO(ask-adam-backend): connection details for Adam's knowledge base,
# once defined (vector store, API, etc.)
ASK_ADAM_KNOWLEDGE_BASE_URL=
